Project migrated to Next.js fullstack mode.

- Removed FastAPI runtime dependency from active stack.
- API endpoints now live in `frontend/pages/api`.
- Dockerfile moved to project root.
- `docker-compose.yml` now runs Next.js app + PostgreSQL.
